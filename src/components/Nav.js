// This file exists for backward compatibility. It re-exports the
// default export from the `.jsx` implementation so that imports using
// `./components/Nav` continue to work without modification.
export { default } from './Nav.jsx';
